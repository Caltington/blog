<h1>HELLOOOOOOOOO</h1>

This blog has <%= Article.public_count %> articles available

<ul>
    <% @articles.each do |article| %>
      <% unless article.archived? %>
      <li>
        <%= link_to article.title, article %>
      </li>
      <li>
      <%= article.body %>
      </li>
    <% end %>
  <% end %>
</ul>

<%= link_to "New Article", new_article_path %>